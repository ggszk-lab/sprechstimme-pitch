# Data

## Metadata CSVs (bundled in `data/metadata/`)

- `score_events.csv` — score-derived events (pitch, onset, duration)
- `segments.csv` — annotated voice segments per recording
- `segment_score_map.csv` — mapping between segments and score events

License: CC BY 4.0.

*Schemas to be documented as the CSVs are imported from the private
research repository.*

## Audio

Not bundled. See [`../LEGAL_NOTICE.md`](../LEGAL_NOTICE.md) and
[`../scripts/fetch_audio.py`](../scripts/fetch_audio.py).
