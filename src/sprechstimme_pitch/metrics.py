"""Three-axis metrics: register, range, contour.

To be ported from the private research repository.

Planned API
-----------
- register_offset(pitch_obs, pitch_score) -> float
- range_compression(pitch_obs, pitch_score) -> float
- contour_correlation(pitch_obs, pitch_score) -> float
"""
